[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Astro outputs a fully static site; no redirects needed for the SPA fallback.
# Pagefind search assets are emitted into dist/ by the Starlight build.
